package com.sudoku.Solver.check;

import com.sudoku.board.elements.SudokuBoard;

public class BlockCheck implements Check {

    @Override
    public SudokuBoard check(SudokuBoard board) {
        return null;
    }
}
